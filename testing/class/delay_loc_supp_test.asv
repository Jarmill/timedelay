mset clear

lags = [1; 2];

n = 3;
mpol('t', 1, 1);
mpol('x', n, 1);
mpol('xd', n, 2);

vars = struct('t', t, 'x', x, 'x_lag', xd);

X = [sum(x) <= 1; x >= 0];
X_init = 

Tmax = 20;

%SLIR model
%susceptible, latent, infectious, removed

beta = 5;
gamma = 1;
alpha = 20;

%xd(3, 1) infected 
f = [-beta*x(1)*x(3);
     beta*xd(1, 1)*xd(3, 1) - alpha*xd(2, 2);
     alpha*xd(2, 2) - gamma*x(3)];
 
%  lsupp = delay_loc_support(vars);
 lsupp.X = X;
 lsupp.lags = lags;
 lsupp.vars = vars;
 